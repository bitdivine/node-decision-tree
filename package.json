{
  "name": "fast-decision-tree",
  "version": "0.0.1",
  "description": "Minimise entropy",
  "main": "lib/decision-tree.js",
  "scripts": {
    "test": "npm run lint && node test/unweighted.js && node test/weighted.js && echo OK",
    "lint": "find lib test | grep -E '.js$' | xargs jshint",
    "start": "node lib/decision-tree.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/bitdivine/node-decision-tree.git"
  },
  "author": "",
  "license": "BSD",
  "dependencies": {
  }
}
